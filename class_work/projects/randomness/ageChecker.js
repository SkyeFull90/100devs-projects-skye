/*function ageChecker(){
    

    if (age < 16) {
        console.log("You can drive")
    }
    else if(age < 18){
        console.log("You can't hate from outside the club, because you can't get in")
    } 

    else if(age < 21){
        console.log("You can drink")
    } 
    else if(age < 25){
        console.log("You can't hate from outside the club, because you can't get in")
    } 
    else if(age < 30){
        console.log("You can't hate from outside the club, because you can't get in")
    } 
    else {
        console.log("You old")
    } 
}
*/
const birthYear = 2000;
const isAdult = birthYear;

if (isAdult) {
  console.log("You are of legal age.");
} else {
  console.log("You are not yet of legal age.");
}