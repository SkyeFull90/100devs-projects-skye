// Create a function that divides one num by another and returns the remainder

function divide(num1, num2) {
    return num1 / num2;
}
console.log(divide(10, 5));

// create a function that adds two numbers and if the sum is greater than 5 log the result

function add(num1, num2) {
    if (num1 + num2 > 5) {
        console.log(num1 + num2);
    }
}
add(10, 5);
